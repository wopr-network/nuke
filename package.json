{
  "name": "@wopr-network/nuke",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/worker-runtime",
    "test": "npm run test -w packages/worker-runtime",
    "check": "npm run check -w packages/worker-runtime"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
